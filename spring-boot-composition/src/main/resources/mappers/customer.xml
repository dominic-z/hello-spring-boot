<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.customers">
    <select id="select_customers" resultType="Customer">
        select *
        from customers
    </select>

    <select id="select_with_where" resultType="Customer">
        select * from customers
        <where>
                addressLine2 != null
<!--            <if test="ids != null">-->
<!--                <trim prefixOverrides="WHERE|where" prefix="and (" suffix=")">-->
<!--                    <where>-->
<!--                        <if test="true">-->
<!--                            customerNumber in-->
<!--                            <foreach collection="ids" item="id" open="(" close=")" separator=",">-->
<!--                                #{id}-->
<!--                            </foreach>-->
<!--                        </if>-->
<!--                    </where>-->
<!--                </trim>-->
<!--            </if>-->
        </where>
    </select>
</mapper>